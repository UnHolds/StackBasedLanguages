services:
  gforth:
    build: base/
    image: gforth/base
    container_name: gforth

  safe-gforth:
    build: safe/
    image: gforth/safe
    container_name: gforth-safe
    ports:
      - 8080:8080
